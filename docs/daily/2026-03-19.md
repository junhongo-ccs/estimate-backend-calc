# Daily Log - 2026-03-19

## Work Done

- Continued the ODC work directly on Mac.
- Confirmed the `Department` selector now uses a real `Dropdown` on `EstimateForm`.
- Added and wired the screen aggregate:
  - `GetDepartmentMasters`
- Confirmed the aggregate configuration:
  - source: `DepartmentMaster`
  - filter: `DepartmentMaster.Is_Active = True`
  - sorting: `DepartmentMaster.Order (ASC)`
- Corrected the dropdown value contract so the backend receives the formal department name string:
  - `Dropdown1.Options Text = DepartmentMaster.DisplayName`
  - `Dropdown1.Options Value = DepartmentMaster.DisplayName`
- Corrected the client variable type mismatch by setting:
  - `Client.Department` -> `Text`
- Populated `DepartmentMaster` with the live BS department list used by the Python logic.
- Published and verified that the department dropdown renders and shows the formal department names.
- Verified in the browser that changing department changes the estimate result when departments have different coefficients.
- Removed the unused `ParseEstimateResponse` client action and the related warnings.

## What Was Achieved

- The department dropdown is now live in the trusted Mac-side ODC app.
- The backend receives the exact formal department string required by the current API contract.
- The estimate result is now department-sensitive in the browser.
- `DepartmentMaster` is now the live source of truth for selectable departments in ODC.
- The old warning noise from the unused parsing action is gone.

## Important Implementation Details

- `Client.Department` must stay `Text`.
- `Dropdown1.Options Value` must stay `DepartmentMaster.DisplayName`.
- Do not switch the dropdown value to `DepartmentMaster.Id` unless the backend contract is changed too.
- The first three business innovation departments share the same backend coefficients, so switching only within those may not visibly change the result.

## Current Working End State

- `EstimateForm` includes:
  - `Department` dropdown
  - `Screen Count`
  - `Table Count`
  - `Run Estimate`
  - `Estimated Amount`
  - `Total Man Days`
  - `Estimated Range`
- `DoTestCalculate` still calls `GetCalculateSimpleGet`.
- `DepartmentMaster` contains 19 active department records.
- Browser behavior is confirmed for:
  - department change
  - screen count change
  - table count change

## Next Step For The Next Session

1. Keep the current `department` flow unchanged.
2. Treat the current dropdown wiring and `DepartmentMaster` data as the baseline.
3. Move to the next business input or calculation enhancement.
