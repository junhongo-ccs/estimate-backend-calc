# Daily Log - 2026-03-17

## Work Done

- Resumed from the already-working dynamic `GetCalculateSimpleGet` flow in ODC.
- Added a new ODC client variable:
  - `Client.Department`
- Added a `Department` input field on `EstimateForm`.
- Extended the backend in `/Users/hongoujun/Documents/GitHub/estimate-backend-calc/outsystems_api_wrapper.py` so these routes accept `department`:
  - `GET /calculate_simple_get`
  - `POST /calculate_simple`
- Deployed the backend update to Railway.
- Confirmed by direct request that a valid formal department name changes the result.
- Updated the ODC REST method definition for `GetCalculateSimpleGet`:
  - added input parameter `department`
  - updated URL path to include `&department={department}`
- Wired `DoTestCalculate` so ODC now passes:
  - `Client.ScreenCount`
  - `Client.TableCount`
  - `Client.Department`
- Verified in the browser that changing department affects the estimate.
- Tried to move `Department` to a proper dropdown backed by ODC data.
- Confirmed that the first dropdown attempt was too heavy and error-prone in the current ODC flow.
- Replaced free-text `Department` with a temporary `Radio Group` using a few fixed formal choices.
- Bound the temporary `Radio Group` to `Client.Department`.
- Verified that the temporary radio-based selector works end-to-end.

## What Was Achieved

- `department` is now part of the live ODC-to-backend flow.
- The PoC is no longer limited to only count-based changes.
- The app can now prove that department selection changes the estimation result.
- Free-text department input is no longer the preferred direction.

## Problems Encountered

- ODC navigation remained fragile and easy to lose.
- ODC REST method updates were not refresh-friendly, so the `department` parameter had to be added manually.
- ODC dropdown setup was much harder than expected in the current screen context.
- A first attempt using `DepartmentMaster` as an entity did not cleanly produce the simple selectable list needed by the dropdown.
- ODC expression editors were strict:
  - string values needed quotes
  - leftover attributes on text widgets caused unrelated property errors

## Resolution Strategy Chosen

- Keep the working `DoTestCalculate` flow intact.
- Prioritize a working end-to-end `department` parameter over UI polish.
- Use a temporary `Radio Group` for department selection instead of staying blocked on dropdown wiring.
- Treat the radio group as an intermediate step only, not the final UI.

## Working End State At The End Of The Day

There is now an ODC screen with:

- temporary `Department` selector
- `Screen Count` input
- `Table Count` input
- `Run Estimate` button
- `Estimated Amount`
- `Total Man Days`
- `Estimated Range`

The screen sends all three live values to `GetCalculateSimpleGet`:

- `department`
- `screen_count`
- `table_count`

One confirmed department-sensitive case is:

- `department = ＣＳ第１システム開発部`
- `screen_count = 4`
- `table_count = 7`
- the result differs from the previous default-department result

## Next Step For The Next Session

Replace the temporary `Department` radio selector with a proper dropdown.

Requirements for that next step:

- use formal department names only
- do not return to free-text department input
- keep `Client.Department` as the bound target
- keep `DoTestCalculate` and `GetCalculateSimpleGet` wiring intact

## Resume Reminder

When resuming:

1. Do not remove the current working `department` flow first
2. Do not go back to free-text department input
3. Treat the current radio selector as temporary
4. The next UI goal is a dropdown because the real department count is 10+
