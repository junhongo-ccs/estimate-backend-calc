# Daily Log - 2026-03-13

## Work Done

- Installed Railway CLI and used it to support backend iteration.
- Added and deployed:
  - `GET /calculate_test`
  - `POST /calculate_simple`
  - `GET /calculate_simple_get`
- Confirmed Railway production endpoint behavior.
- Fixed UTF-8 JSON response handling to eliminate Japanese text corruption in ODC.
- Proved first ODC-to-Railway end-to-end flow using `calculate_test`.
- Displayed key estimation values on the ODC screen.
- Moved from fixed test endpoint toward a dynamic route using `calculate_simple_get`.
- Consumed `GetCalculateSimpleGet` successfully in ODC.
- Switched the active ODC action from the old test GET to `GetCalculateSimpleGet`.
- Set fixed query values in ODC:
  - `screen_count = 12`
  - `table_count = 4`
- Reconfirmed visible output using the dynamic GET route instead of the fixed test route.

## What Was Achieved

- The migration is technically viable.
- ODC can already render real estimation output from the Railway backend.
- The team no longer depends on only fixed mock display.
- The current PoC is past the “can this connect at all?” stage.

## Problems Encountered

- ODC POST request structure binding repeatedly caused friction.
- Type pickers were inconsistent in several contexts.
- UI layout behavior in `MainContent` was not intuitive.
- Instructions had to be reduced to exact tabs and single operations to stay reliable.

## Resolution Strategy Chosen

- Avoid rich POST binding for now.
- Use GET query parameters for the immediate PoC.
- Keep the scope focused on a stable dynamic input loop.

## Working End State At The End Of The Day

There is an ODC screen with:

- `Run Estimate` button
- `Estimated Amount`
- `Total Man Days`
- `Estimated Range`

These values are already populated from the backend path that has been made ODC-friendly.

The currently visible case is:

- `screen_count = 12`
- `table_count = 4`
- `Estimated Amount = 31921098`
- `Total Man Days = 453`
- `Estimated Range = ¥27,132,933 〜 ¥36,709,262`

## Next Step For The Next Session

Make the screen values dynamic by introducing user-editable inputs for:

- `screen_count`
- `table_count`

Then wire them into:

- `GetCalculateSimpleGet.screen_count`
- `GetCalculateSimpleGet.table_count`

and verify the displayed result changes when the user changes inputs.

## Resume Reminder

When resuming:

1. Do not go back to `POST /calculate` first
2. Do not go back to `POST /calculate_simple` first
3. Continue from the already-working `GetCalculateSimpleGet` flow
4. Only after dynamic input works, consider returning to richer POST routes
