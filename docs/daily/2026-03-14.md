# Daily Log - 2026-03-14

## Work Done

- Opened the already-working `EstimateForm` flow in ODC and resumed from the `GetCalculateSimpleGet` path.
- Added two actual input widgets to the screen:
  - `Screen Count`
  - `Table Count`
- Created ODC client variables:
  - `ScreenCount`
  - `TableCount`
- Bound the two input widgets to:
  - `Client.ScreenCount`
  - `Client.TableCount`
- Set the input type to numeric for both fields.
- Kept the existing action flow structure unchanged:
  - `GetCalculateSimpleGet`
  - `JSON Serialize`
  - `Assign`
  - `Assign`
- Updated `DoTestCalculate` so the REST call no longer uses fixed values.
- Replaced the fixed query parameters with:
  - `Client.ScreenCount`
  - `Client.TableCount`
- Published and verified that changing input values changes the displayed estimate.
- Improved the visible screen layout enough for continued testing:
  - two inputs shown on one row
  - labels added above each input

## What Was Achieved

- The PoC is now truly dynamic for the two count inputs.
- ODC is no longer limited to the fixed `12 / 4` test case.
- The current UI-to-backend-to-screen update loop works with user-entered values.

## Problems Encountered

- ODC navigation was easy to lose track of once multiple canvases and panes were open.
- Search behavior in ODC was inconsistent depending on which pane or element type was active.
- Some validation errors were indirect:
  - missing `Input.Variable` bindings showed up as general required-property errors
  - a text-label mistake produced a separate invalid-expression error
- Layout work in `MainContent` remained sensitive and unintuitive.

## Resolution Strategy Chosen

- Continue using exact tab names and one-step instructions only.
- Avoid changing the working action-flow structure while making UI changes.
- Fix errors by locating the exact widget or action node first instead of inferring from the message text alone.
- Accept minimal UI polish for now and prioritize working end-to-end behavior.

## Working End State At The End Of The Day

There is now an ODC screen with:

- `Screen Count` input
- `Table Count` input
- `Run Estimate` button
- `Estimated Amount`
- `Total Man Days`
- `Estimated Range`

These values are populated from the backend through `GetCalculateSimpleGet`, and the two query parameters are now driven by user input.

One verified visible case is:

- `screen_count = 4`
- `table_count = 7`
- `Estimated Amount = 21351198`
- `Total Man Days = 303`
- `Estimated Range = ¥18,148,518 〜 ¥24,553,877`

## Next Step For The Next Session

Keep the current dynamic GET flow working and expand the business inputs gradually.

Recommended first next input:

- `department`

Then continue by:

- extending the backend contract
- wiring the new input into ODC
- confirming the result still updates correctly

## Resume Reminder

When resuming:

1. Do not roll back to fixed `12 / 4`
2. Do not switch back to POST-first work
3. Start from the already-working dynamic `GetCalculateSimpleGet` screen
4. Add the next business parameter in the smallest safe step
