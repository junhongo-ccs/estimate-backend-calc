# Daily Log - 2026-03-18

## Work Done

- Resumed from the `Department` radio-to-dropdown migration task in ODC.
- Confirmed the existing temporary `Radio Group` was bound to:
  - `Client.Department`
- Added a new `Dropdown` on `EstimateForm`.
- Confirmed the dropdown should continue sending the formal department name string, not an ID, because the backend expects the exact department name.
- Investigated multiple ODC list-binding approaches:
  - first tried a local text-list variable
  - then switched to a screen aggregate because the dropdown expected record attributes
- Added a screen aggregate:
  - `GetDepartmentMasters`
- Configured `GetDepartmentMasters` with:
  - source: `DepartmentMaster`
  - filter: `DepartmentMaster.Is_Active = True`
  - sorting: `DepartmentMaster.Order (ASC)`
- Wired the dropdown as:
  - `Dropdown1.Variable = Client.Department`
  - `Dropdown1.List = GetDepartmentMasters.List`
  - `Options Text = DepartmentMaster.DisplayName`
  - `Options Value = DepartmentMaster.DisplayName`
- Confirmed the local OML now contains the intended department dropdown implementation.
- Attempted `1-Click Publish` from Windows ODC Studio.
- Observed that Windows ODC Studio saves and uploads a new revision, but publish ends with:
  - `Your current role doesn't allow you to perform this action in this app. (Forbidden)`
- Confirmed this was not simply a browser-access problem:
  - browser access to `personal-pvxsnkrq.outsystems.dev` still works
- Investigated Windows environment/network factors:
  - confirmed `Zscaler Client Connector` is installed on the Windows machine
  - temporarily turned Zscaler private access off during troubleshooting
- Compared Windows and Mac behavior:
  - Mac ODC Studio did not show the same Windows-only publish symptom previously
  - Mac opening the app normally still showed the older server-side version
- Later verification showed that Mac also cannot simply open/use the Windows-created `.oml` handoff as the practical continuation path.
- Identified the latest local OML artifact as:
  - `C:\Users\hongouj\OneDrive - NTT DATA\ドキュメント\OSML\AI Estimation System.oml`
- Created a Git handoff path so the latest local OML can be opened from Mac:
  - added `OML/AI Estimation System.oml` to this repository
  - created branch `codex/odc-dropdown-transfer`
  - committed and pushed the OML handoff branch

## What Was Achieved

- The intended `Department` dropdown wiring was identified correctly.
- The required formal-name binding path was documented.
- The Windows attempt established the historical troubleshooting context for the later Mac implementation.

## Problems Encountered

- Windows ODC Studio publish ends with `Forbidden` even after the local changes are saved.
- Windows and Mac were not showing the same effective working version at first.
- The screen aggregate preview showed `403 - Forbidden`, making preview-based validation unreliable.
- ODC conflict/override behavior was not enough to guarantee that the latest Windows-local version became the server-side latest version.
- Mac could not be treated as a reliable consumer of the Windows-generated OML handoff for this task.

## Resolution Strategy Chosen

- Treat the Windows attempt as a factual reconstruction source, not as the future working environment.
- Preserve the attempted dropdown wiring in docs so it can be recreated directly on Mac.
- Do all future ODC work on Mac only.

## Working End State At The End Of The Day

At the end of 2026-03-18, the working implementation was not yet live on the trusted Mac-side server state.

What was known at that point:

- the correct dropdown design used `Client.Department`
- the screen aggregate should be `GetDepartmentMasters`
- the dropdown needed `DepartmentMaster.DisplayName` for both text and value
- Windows publish remained blocked by `Forbidden`
- the Windows `.oml` handoff was historical context only, not the final trusted runtime state

## Next Step For The Next Session

On Mac:

1. Open the current server-side app version of `EstimateForm`
2. Recreate the dropdown wiring directly in the app
3. Publish from Mac ODC Studio
4. Verify in the browser that:
   - `Department` is a dropdown
   - formal department names appear
   - `Run Estimate` still updates the result correctly

## Resume Reminder

When reading this entry later:

1. Treat it as historical context from the pre-Mac-fix stage
2. Use `docs/status/master_status.md` as the current truth
3. Use `docs/odc/odc_notes.md` for the current working wiring
